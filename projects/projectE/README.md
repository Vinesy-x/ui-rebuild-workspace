# projectE · 大掌柜

> 现代版 BitLife 类放置养成游戏视频帧 → 宋朝古风(江南水乡水墨彩绘)Vue 工程重建。

## 当前阶段

看 [`PROGRESS.md`](PROGRESS.md)。Phase B T-B2 ✅ / T-B3 待启动。

## 跑工程

```bash
cd final && npm install && npm run dev
# 浏览器打开 http://localhost:5173
```

## 视觉真值参考

`preview/`:
- `Phase A · 主菜单 大掌柜.html` — fps_0035 主菜单完整视觉
- `Style Lock · 风格定档.html` — 风格规约(色板 / 字号 / 货币印章 / 角标 / 27 古风对照)
- `大掌柜.html` — Phase A + T-B1~T-B4 合并 HTML 真值预演

## 入口

- `KICKOFF.md` — Phase A 首次启动 prompt(已过 Phase A 锁定)
- `tasks/` — 各屏 task prompt(累积)
- `bugs/` — bug 报告(累积)
- `design-brief/flows/` — canonical 屏映射 / 交互 / 拓扑(Claude 维护)
- `design-brief/analysis/` — 50 张 PNG 抽帧高清

## workspace 级文档

跨项目通用规约 / 模板见 workspace 根的 `docs/` 和 `scripts/`。
